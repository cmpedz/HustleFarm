using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimalDataChangeManager : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private List<GameObject> animalsHas = new List<GameObject>();
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
